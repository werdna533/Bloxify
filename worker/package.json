{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute htn-lab --local --file=./migrations/0001_init.sql && wrangler d1 execute htn-lab --local --file=./migrations/0002_image_after_key.sql",
    "migrate:remote": "wrangler d1 execute htn-lab --remote --file=./migrations/0001_init.sql && wrangler d1 execute htn-lab --remote --file=./migrations/0002_image_after_key.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
